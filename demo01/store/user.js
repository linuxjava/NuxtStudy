export const state = () => ({
  username: '张珊'
})

export const getter = () => ({

})

export const mutations = {
  changeUsername(state, name) {
    state.username = name
  }
}

export const actions = {

}
